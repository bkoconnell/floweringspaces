Change Log for Portfolio Submission



*4/23/2021*
    --added additional pages (no content)
        --routers & controllers
        --update app.js
        --hbs navigation / partials
    --refactored app_server routing
        -- consolidated all server routers to index.js
        -- updated app.js to reflect router changes
            -- changed instantiations
            -- changed app.use functions
                --redirected /nursery to indexRouter
                --added /landscapes (to indexRouter)
    --fixed bug on HBS partials for index page link
    --added HBS functionality for nav bar on backend server
        --app.js ==> registered hbs helper "ifequal" w/ conditional function
                        to use in header/footer partials (selected/active Links)
    --redesigned homepage
        -- swapped in partials
        -- changed text & links
        -- changed pictures, logo, etc.
